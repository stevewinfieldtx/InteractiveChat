# ─── Core LLM + research ──────────────────────────────────────────────────────
OPENROUTER_API_KEY=sk-or-v1-...
OPENROUTER_MODEL=anthropic/claude-haiku-4-5

# TDE (Targeted Decomposition Engine) — primary company/industry research
TDE_API_URL=https://targeteddecomposition-production.up.railway.app
TDE_API_KEY=...

# Tavily — fallback research only (used if TDE is unreachable)
TAVILY_API_KEY=tvly-...

DATABASE_URL=postgresql://user:password@host:5432/dbname

# ─── ElevenLabs ───────────────────────────────────────────────────────────────
ELEVENLABS_AGENT_ID=agent_5101kty2ztmme25aspqycwp7mpsm
ELEVENLABS_API_KEY=sk_...   # server-side, used to poll the live transcript

# ─── Human handoff via email (the "ping the sales team" + brief) ───────────────
SALES_TEAM_EMAIL=stevewinfieldtx@gmail.com
SALES_REP_NAME=Steve
EMAIL_FROM=Guardz Sales Bot <onboarding@resend.dev>
HANDOFF_SEND_AVAILABILITY=true

# Pick ONE email provider:
# Provider A — Resend (recommended, fastest). Create a key at resend.com.
#   Sending FROM onboarding@resend.dev works with no domain setup for testing.
RESEND_API_KEY=re_...
# Provider B — SMTP (e.g. Gmail App Password). Leave RESEND_API_KEY blank to use this.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=stevewinfieldtx@gmail.com
SMTP_PASS=your-16-char-gmail-app-password

# ─── Optional: legacy Slack ping (off if blank) ───────────────────────────────
SLACK_WEBHOOK_URL=
